# Flask Configuration
FLASK_ENV=production          # Set to "development" for local development
FLASK_DEBUG=False             # Set to "True" for debugging in development
PORT=5000                     # Port for the Flask server

# Database Configuration
DATABASE_PATH=elderly_care.db  # Replace with the actual path to your SQLite database

# Logging Configuration
LOG_LEVEL=INFO                # Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)

# Other Configuration (if needed)
SECRET_KEY=your_secret_key    # Replace with a secure secret key for Flask sessions